extends layout

block content
    section#hero__content.hero
        .inner
            .hero__section
                .hero__title
                    h1 Welcome To <span class="red">Event</span><span class="hop">Hop</span>
                .hero__subtitle
                    h2 Let's <span class="red">Do </span>Something
                .hero__click
                    a.hero__click--anchor(href="#popGroups") Durham Style . . .
    
    section#about__content.about
        .inner
            .about__section
                .about__section--title
                    h2.about__title About
                    h3.about__subtitle Do It Like Durham!
                .about__section--cards
                    .about__section--card
                        .about__section--card-icon
                        .about__section--card-title
                        .about__section--card-text
                    .about__section--card
                        .about__section--card-icon
                        .about__section--card-title
                        .about__section--card-text
                    .about__section--card
                        .about__section--card-icon
                        .about__section--card-title
                        .about__section--card-text

    section#featEvents__content.featuredEvents
        p EVENTS EVENTS EVENTS
        block featEvents

    section#mainCreate__content.mainCreate
        p CREATE CREATE CREATE
        block mainCreate

    section#popGroups__content.popularGroups
        p GROUPS GROUPS GROUPS
        block popGroups
    