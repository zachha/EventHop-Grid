extends layout

include mixins/_loginForm
include mixins/_forgotForm

block content
    section#login__content.login
        .l-flex-row.login__inner
            .login__section
                +loginForm()
                +forgotForm()