mixin forgotForm()
    form.form.container.form__forgot(action="/account/forgot" method="POST")
        h2.form__title I forgot my password!
        label(for="email") Email Address
        input(type="email" name="email" placeholder="Email Address" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false" required)
        input.button.submit__button(type="submit" value="Reset")