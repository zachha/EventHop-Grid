mixin loginForm()
    form.form.form__login(action="/login" method="POST")
        h2.form__title Login
        label(for="loginEmail") Email Address
        input(type="email" name="email" id="loginEmail" placeholder="Email Address" autocomplete="username" autofocus required)
        label(for="loginPass") Password
        input(type="password" name="password" id="loginPass" placeholder="Password" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false" required)
        input.button.submit__button(type="submit" name="submit" value="Log In")
            